{{!-- NAVBAR START --}}
{{#paper-toolbar}}
  <div class="md-toolbar-tools">
    <div class="left"> 
      {{#paper-button class="jet-logo" action=(transition-to 'memes')}}<i class="fa fa-hashtag"></i>{{/paper-button}}
      {{memes-lettering}}
    </div>
    <div class="right"> 
      {{#link-to "upload" class="upload"}}Upload{{/link-to}}
    </div>
  </div>
{{/paper-toolbar}}
{{!-- NAVBAR END --}}