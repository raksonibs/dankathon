import promise from 'frontend/utils/promise';
import { module, test } from 'qunit';

module('Unit | Utility | promise');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = promise();
  assert.ok(result);
});
