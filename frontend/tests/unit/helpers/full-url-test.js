import { fullUrl } from 'frontend/helpers/full-url';
import { module, test } from 'qunit';

module('Unit | Helper | full url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fullUrl([42]);
  assert.ok(result);
});
