import { imageExists } from 'frontend/helpers/image-exists';
import { module, test } from 'qunit';

module('Unit | Helper | image exists');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = imageExists([42]);
  assert.ok(result);
});
